import { inputReaderHelper } from '#lib/input-reader'

/**
 * Merge sort.
 * 
 * @param {any} arg1
 * @param {any} arg2
 * ...
 * @param {any} argN
 * 
 * @returns {any}
 */
var sortMerge = function (arg1) {
    let output = null
    // Solution for a single case goes here.

    // EXAMPLE
    output = "42: " + arg1

    return output
}

inputReaderHelper(sortMerge)
