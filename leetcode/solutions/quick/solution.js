import { inputReaderHelper } from '#lib/input-reader'

/**
 * Quick sort.
 * 
 * @param {any} arg1
 * @param {any} arg2
 * ...
 * @param {any} argN
 * 
 * @returns {any}
 */
var sortQuick = function (arg1) {
    let output = null
    // Solution for a single case goes here.

    // EXAMPLE
    output = "42: " + arg1

    return output
}

inputReaderHelper(sortQuick)
