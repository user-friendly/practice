import { inputReaderHelper } from '#lib/input-reader'

/**
 * Bucket sort.
 * 
 * @param {any} arg1
 * @param {any} arg2
 * ...
 * @param {any} argN
 * 
 * @returns {any}
 */
var sortBucket = function (arg1) {
    let output = null
    // Solution for a single case goes here.

    // EXAMPLE
    output = "42: " + arg1

    return output
}

inputReaderHelper(sortBucket)
